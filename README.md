# Call-Logs API (NestJS)

A RESTful API built using **NestJS** and **MongoDB** implementing **JWT authentication**

## Features

- Post call logs on a day
- Role-based access 
- JWT authentication (access & refresh tokens)
- View all posted call logs and also by date
- MongoDB connection (via Mongoose)
- Deployed on Render

## 🧩 Tech Stack

- **NestJS**
- **JWT Authentication**
- **MongoDB (Atlas)**
- **Mongoose**
- **Render Deployment**

## 🔑 Environment Variables

```env
PORT=
MONGODB_URI=
JWT_ADMIN_ACCESS_SECRET_KEY=    
JWT_ADMIN_REFRESH_SECRET=
JWT_EMPLOYEE_ACCESS_SECRET_KEY=
JWT_EMPLOYEE_REFRESH_SECRET=
JWT_ACCESS_EXPIRY=
JWT_REFRESH_EXPIRY=
```
